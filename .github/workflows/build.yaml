name: Build Bundle on Push

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm i

      - name: Run Gulp build
        run: npx gulp bundle

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.sha:0:7 }}
          files: build/ui-bundle.zip
